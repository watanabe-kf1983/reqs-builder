entities:
  - id: user
    name: ユーザー
    fields:
      - name: id
        type: string
        pk: true
      - name: email
        type: string
      - name: status
        type: string

  - id: order
    name: 注文
    fields:
      - name: id
        type: string
        pk: true
      - name: user_id
        type: string
        fk: user
      - name: total
        type: number
