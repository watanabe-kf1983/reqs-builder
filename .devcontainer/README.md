# Dev Container

## 環境仕様

- **Node.js**: 20.x (LTS)
- **Package Manager**: npm

## 使い方

1. VSCode で「Reopen in Container」を選択
2. 初回は自動的に Claude Code CLI がインストールされる
3. Claude Code も通常通り使用可能

## ローカル開発（Dev Container なし）

```bash
# nvm で Node.js をインストール
nvm install
nvm use

# 依存をインストール
npm install
```
